import { TextField } from '@mui/material';

const StreetInput = (): JSX.Element => {
  return <TextField type="text" label="Street" className="form__street" variant="standard" />;
};

export default StreetInput;
