import { TextField } from '@mui/material';

const FirstNameInput = (): JSX.Element => {
  return <TextField type="text" label="First Name" className="form__first-name" variant="standard" />;
};

export default FirstNameInput;
