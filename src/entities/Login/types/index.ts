type SignInProps = {
  onClick?: React.MouseEventHandler<HTMLAnchorElement>;
};

type SignUpProps = {
  onClick?: React.MouseEventHandler<HTMLAnchorElement>;
};

export type { SignInProps, SignUpProps };
